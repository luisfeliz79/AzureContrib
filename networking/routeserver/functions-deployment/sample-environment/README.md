# Requirements

The Azure Function Identity must have the following permissions:
- **Contributor** role on the Route Server resource.
- **Monitoring Metrics Publisher** role on the Data Collection Rule
