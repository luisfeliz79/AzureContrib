# `Config` folder

Place a javablob.config file here if you want to include on the container

Sample:

```json
{
    "containerName":  "testdata",
    "storageBlobEndpoint":  "https://<SANAME>.blob.core.windows.net/",
    "numberOfDownloads":  20
}
```